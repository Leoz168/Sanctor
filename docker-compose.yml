services:
  backend:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: sanctor-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - GO_ENV=production
      - PORT=8080
    networks:
      - sanctor-network
    restart: unless-stopped

  frontend:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: sanctor-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://localhost:8080
    networks:
      - sanctor-network
    restart: unless-stopped

networks:
  sanctor-network:
    driver: bridge
