# Sanctor Web

Next.js frontend for Sanctor.

## Development

```bash
npm install
npm run dev
```

The app runs on `http://localhost:3000`.

Set `NEXT_PUBLIC_API_URL` to point at the backend API.
Set `NEXT_PUBLIC_GOOGLE_CLIENT_ID` to enable Google sign-in.
